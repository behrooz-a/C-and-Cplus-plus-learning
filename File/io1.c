#include <stdio.h>

/*  End Of File (EOF) is an integer constant in the <stdio.h> which the value is -1. It demonstrates the end of the file and indicates to the program that there is nothing else following in a file 
    Where can we use it?:  1. Debugging 2. Data Validation 3. comparing characters 4. Detecting the end of a file*/

int main(){

    printf(" %d",EOF);
    return 0;
}