# include <stdio.h>
/* C obeys its predecessor of b programming language since the string data type has not implemented in the C. Then we will employ a way to a hold string
which is to employ character arrays instead of string data type */

int main(){
    char word[10];
    word ="Hello";
    puts(word);
    return 0;} 

